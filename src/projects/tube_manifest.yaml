driver:
  color:
    left: [1.0, 0.0, 0.0, 1.0]
    right: [0.0, 1.0, 0.0, 1.0]
  config:
    modes: [default, mount, text]
  diagram:
    modes: [default]
  part:
    modes: [default, bare, print]
    subassemblies: [left, right]
  wire:
    modes: [default]
  sketch:
    modes: [default]
    subassemblies: [left, right]
passenger:
  color:
    left: [1.0, 0.0, 0.0, 1.0]
    right: [0.0, 1.0, 0.0, 1.0]
  config:
    modes: [default, mount, text]
  diagram:
    modes: [default]
  part:
    modes: [default, bare, print]
    subassemblies: [left, right]
  wire:
    modes: [default]
  sketch:
    modes: [default]
    subassemblies: [left, right]
part_positions:
  view:
    modes: [default]
overlay:
  view:
    modes: [default]
tube_profile:
  view:
    modes: [default]