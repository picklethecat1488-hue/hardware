driver:
  color:
    left: red
    right: green
  config:
    modes: [mount, text]
  part:
    modes: [default, print]
    subassemblies: [left, right]
passenger:
  color:
    left: blue
    right: magenta
  config:
    modes: [mount, text]
  part:
    modes: [default, print]
    subassemblies: [left, right]
product:
  diagram:
    modes: [default]
part_positions:
  view:
    modes: [default]
overlay:
  view:
    modes: [default]
wire:
  view:
    modes: [default]
sketch:
  view:
    modes: [default]