driver:
  color:
    left: red
    right: green
  config:
    modes: [mount, text]
  diagram:
    modes: [default]
  part:
    modes: [default, print]
    subassemblies: [left, right]
passenger:
  color:
    left: blue
    right: magenta
  config:
    modes: [mount, text]
  diagram:
    modes: [default]
  part:
    modes: [default, print]
    subassemblies: [left, right]
part_positions:
  view:
    modes: [default]
overlay:
  view:
    modes: [default]
wire:
  view:
    modes: [default]
sketch:
  view:
    modes: [default]